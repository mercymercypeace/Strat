-- Load Rayfield Library securely to prevent EOF nil errors
local fetchSuccess, fetchResult = pcall(function()
    return game:HttpGet('https://sirius.menu/rayfield')
end)
if not fetchSuccess then
    error("HTTP GET Failed: " .. tostring(fetchResult))
end

local loadFunc, loadErr = loadstring(fetchResult)
if not loadFunc then
    error("loadstring failed (your executor might be blocking it or returned bad HTML): " .. tostring(loadErr))
end

local Rayfield = loadFunc()

-- Services
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local RunService = game:GetService("RunService")
local player = Players.LocalPlayer

-- ==============================
-- // TBB Auto Stage Module
-- ==============================
local TBB = {}
do
    local CONFIG = {
        DELAY_AFTER_WIN  = 3,
        DELAY_AFTER_LOSE = 3,
        AUTO_RETRY       = true,
        AUTO_NEXT        = true,
        DIFFICULTY       = 1,
        MAX_CHAPTERS     = 5,
        MAX_STAGES       = 30,
    }

    local State = {
        running       = false,
        chapter       = nil,
        stage         = nil,
        stars         = 1,
        wins          = 0,
        losses        = 0,
        conn          = nil,
    }

    TBB.onNotify       = function(title, content, icon) end
    TBB.onStatusChange = function(text) end
    TBB.onStatsChange  = function(text) end

    local function notify(title, content, icon)
        TBB.onNotify(title, content, icon)
    end

    local function setStatus(text)
        TBB.onStatusChange(text)
    end

    local function pushStats()
        local starStr = string.rep("⭐", State.stars) .. string.rep("☆", 3 - State.stars)
        TBB.onStatsChange(string.format("W: %d  L: %d  |  %s  |  Diff: %d", State.wins, State.losses, starStr, CONFIG.DIFFICULTY))
    end

    local function getProgress()
        if State.chapter and State.chapter ~= "" then
            State.stage = State.stage or 1
            return State.chapter, State.stage
        end

        local ok, unlocked = pcall(function()
            return player:WaitForChild("PlayerData", 5):WaitForChild("Unlocked", 5)
        end)
        if not ok or not unlocked then
            notify("Error", "PlayerData not found", "alert-circle")
            return "Chapter1", 1
        end

        local bestChapter = 1
        local bestStage   = 1

        for i = CONFIG.MAX_CHAPTERS, 1, -1 do
            local chFolder = unlocked:FindFirstChild("Chapter" .. i)
            if chFolder then
                bestChapter = i

                if chFolder:IsA("IntValue") or chFolder:IsA("NumberValue") then
                    bestStage = math.max(chFolder.Value, 1)
                else
                    local sv = chFolder:FindFirstChild("Stage")
                        or chFolder:FindFirstChild("Current")
                        or chFolder:FindFirstChild("Progress")
                    if sv and sv:IsA("ValueBase") then
                        bestStage = math.max(sv.Value, 1)
                    else
                        local highest = 0
                        for _, child in ipairs(chFolder:GetChildren()) do
                            local n = tonumber(child.Name)
                            if n and n > highest then highest = n end
                        end
                        if highest > 0 then bestStage = highest end
                    end
                end
                break
            end
        end

        State.stage = bestStage
        return "Chapter" .. bestChapter, bestStage
    end

    local function printProgress()
        local ok, unlocked = pcall(function() return player:WaitForChild("PlayerData", 5):WaitForChild("Unlocked", 5) end)
        if not ok or not unlocked then return end
        
        print("\n┌─────── 📊 Unlocked Progress ───────")
        for i = 1, CONFIG.MAX_CHAPTERS do
            local ch = unlocked:FindFirstChild("Chapter" .. i)
            if ch then
                local stageText = "?"
                if ch:IsA("IntValue") or ch:IsA("NumberValue") then
                    stageText = tostring(ch.Value)
                else
                    local sv = ch:FindFirstChild("Stage") or ch:FindFirstChild("Current") or ch:FindFirstChild("Progress")
                    if sv then stageText = tostring(sv.Value) end
                end
                print("│  Chapter " .. i .. "  →  Stage " .. stageText)
            end
        end
        print("└────────────────────────────────────\n")
    end

    local function startBattle()
        if not State.running then return end

        local events = ReplicatedStorage:FindFirstChild("Events")
        local remote = events
            and events:FindFirstChild("RemoteFunction")
            and events.RemoteFunction:FindFirstChild("StartBattle")

        if not remote then
            notify("Error", "StartBattle remote missing", "alert-circle")
            setStatus("❌  Remote not found")
            return
        end

        local chapter, stage = getProgress()
        local starStr = string.rep("⭐", State.stars) .. string.rep("☆", 3 - State.stars)

        setStatus(string.format("▶  %s  S%d  %s  D%d", chapter, stage, starStr, CONFIG.DIFFICULTY))
        pushStats()

        print(string.format("🚀 Firing  ›  %s  |  Stage %d  |  Stars %d  |  Diff %d", chapter, stage, State.stars, CONFIG.DIFFICULTY))

        local ok, err = pcall(function()
            -- Invoke with current State.stars based on chapter progression completion
            remote:InvokeServer(chapter, stage, CONFIG.DIFFICULTY, State.stars, false, {}, false, 1)
        end)

        if not ok then
            notify("Error", "Remote failed: " .. tostring(err), "alert-circle")
        end
    end

    local function hookWinLose()
        local winRemote = ReplicatedStorage
            :WaitForChild("Events", 10)
            :WaitForChild("RemoteEvents", 10)
            :WaitForChild("WinOrLose", 10)

        if State.conn then State.conn:Disconnect() end

        State.conn = winRemote.OnClientEvent:Connect(function(isWin)
            if not State.running then return end

            if isWin == true then
                State.wins = State.wins + 1
                
                if State.stage and State.stage < CONFIG.MAX_STAGES then
                    State.stage = State.stage + 1
                    print(string.format("🏆 Win!  ›  Advancing to Stage %d  (in %ds)", State.stage, CONFIG.DELAY_AFTER_WIN))
                    notify("Victory!", string.format("Advancing to Stage %d", State.stage), "trophy")
                else
                    if State.stars < 3 then
                        State.stars = State.stars + 1
                        State.stage = 1
                        print(string.format("🏆 Max Stage Cleared!  ›  Advancing to Star %d  (in %ds)", State.stars, CONFIG.DELAY_AFTER_WIN))
                        notify("Star Up!", string.format("Advancing to Star %d", State.stars), "star")
                    else
                        print("🏆 Win!  ›  Chapter fully completed (3 stars)")
                        notify("Victory!", "Chapter fully completed (3 stars)", "trophy")
                        State.stage = nil
                    end
                end

                pushStats()

                if CONFIG.AUTO_NEXT then
                    task.wait(CONFIG.DELAY_AFTER_WIN)
                    startBattle()
                end

            elseif isWin == false then
                State.losses = State.losses + 1
                print(string.format("💀 Loss  ›  Retrying Stage %d  (in %ds)", State.stage or 0, CONFIG.DELAY_AFTER_LOSE))
                notify("Defeat", string.format("Retrying Stage %d", State.stage or 0), "refresh-cw")
                pushStats()

                if CONFIG.AUTO_RETRY then
                    task.wait(CONFIG.DELAY_AFTER_LOSE)
                    startBattle()
                end
            end
        end)
    end

    function TBB.start()
        if State.running then return end
        State.running = true
        State.wins    = 0
        State.losses  = 0
        printProgress()
        hookWinLose()
        startBattle()
        notify("Auto Farm", "Started", "play")
        setStatus("▶  Running")
    end

    function TBB.stop()
        State.running = false
        if State.conn then State.conn:Disconnect(); State.conn = nil end
        notify("Auto Farm", "Stopped", "square")
        setStatus("⏸  Idle")
        pushStats()
    end

    function TBB.setChapter(name)
        if name == "Auto" or name == nil or name == "" then
            State.chapter = nil
        else
            State.chapter = name
        end
        State.stage = nil
        State.stars = 1
        print("📌 Chapter override set to: " .. tostring(name))
    end

    function TBB.setStage(n)
        State.stage = tonumber(n) or nil
        print("📌 Stage override set to: " .. tostring(State.stage))
    end

    function TBB.setDifficulty(n)
        CONFIG.DIFFICULTY = math.clamp(tonumber(n) or 1, 1, 3)
        pushStats()
    end

    function TBB.getStats()
        return { wins = State.wins, losses = State.losses, stage = State.stage }
    end
end

-- ==============================
-- // TBB Projectiles Exploit Module
-- ==============================
local ProjectilesExploit = {
    running = false,
    connections = {}
}
do
    local bodyAngularList = {}

    local function trackBAV(bav)
        if bav and bav:IsA("BodyAngularVelocity") then
            bodyAngularList[bav] = true
        end
    end

    local function removeTouchInterest(descendant)
        if descendant and descendant:IsA("TouchInterest") then
            descendant:Destroy()
        end
    end

    local function getHighestHPNPC()
        local highestNPC
        local maxHealth = -math.huge
        local npcFolder = Workspace:FindFirstChild("NPCFolders")
        local enemyFolder = npcFolder and npcFolder:FindFirstChild("EnemyFolder")
        
        if enemyFolder then
            for _, npc in ipairs(enemyFolder:GetChildren()) do
                local humanoid = npc:FindFirstChild("Humanoid")
                if humanoid and humanoid.Health > 0 and humanoid.Health > maxHealth then
                    maxHealth = humanoid.Health
                    highestNPC = npc
                end
            end
        end
        return highestNPC
    end

    local function HandleTable(tablePart)
        if not tablePart:IsA("BasePart") then return end
        local npcFolder = Workspace:FindFirstChild("NPCFolders")
        local baseFolder = npcFolder and npcFolder:FindFirstChild("BaseFolder")
        local blueBase = baseFolder and baseFolder:FindFirstChild("Blue Base")
        local blueSpawn = blueBase and blueBase:FindFirstChild("Spawn")
        if not blueSpawn then return end

        tablePart.CFrame = blueSpawn.CFrame * CFrame.new(0, -1, 0)
        tablePart.Size = tablePart.Size * 100000 
        tablePart.CustomPhysicalProperties = PhysicalProperties.new(0.7, 0.3, 0.5)
        
        local bav = tablePart:FindFirstChildOfClass("BodyAngularVelocity")
        if not bav then
            bav = Instance.new("BodyAngularVelocity")
            bav.MaxTorque = Vector3.new(1e6, 1e6, 1e6)
            bav.AngularVelocity = Vector3.new(-40, 0, 0)
            bav.Parent = tablePart
        end
        trackBAV(bav)
        
        for _, desc in ipairs(tablePart:GetDescendants()) do
            removeTouchInterest(desc)
        end
    end

    local function HandleStoneFist(fistPart)
        if not fistPart:IsA("BasePart") then return end
        local targetNPC = getHighestHPNPC()
        if targetNPC then
            local root = targetNPC:FindFirstChild("HumanoidRootPart")
            if root then
                fistPart.CFrame = CFrame.new(root.Position + Vector3.new(0, 5, 0))
            end
        end
    end

    function ProjectilesExploit.start()
        if ProjectilesExploit.running then return end
        ProjectilesExploit.running = true

        local projectileFolder = Workspace:FindFirstChild("Projectile")
        if projectileFolder then
            for _, obj in ipairs(projectileFolder:GetChildren()) do
                if obj.Name == "Table" then
                    HandleTable(obj)
                elseif obj.Name == "StoneFist" then
                    HandleStoneFist(obj)
                end
            end

            local c1 = projectileFolder.ChildAdded:Connect(function(child)
                if not ProjectilesExploit.running then return end
                if child.Name == "Table" then
                    HandleTable(child)
                elseif child.Name == "StoneFist" then
                    HandleStoneFist(child)
                end
            end)
            table.insert(ProjectilesExploit.connections, c1)
        end

        local c2 = RunService.Heartbeat:Connect(function()
            if not ProjectilesExploit.running then return end
            for bav,_ in pairs(bodyAngularList) do
                if bav.Parent then
                    bav.AngularVelocity = Vector3.new(-40, 0, 0)
                else
                    bodyAngularList[bav] = nil
                end
            end
        end)
        table.insert(ProjectilesExploit.connections, c2)
    end

    function ProjectilesExploit.stop()
        ProjectilesExploit.running = false
        for _, conn in ipairs(ProjectilesExploit.connections) do
            if conn then conn:Disconnect() end
        end
        ProjectilesExploit.connections = {}
        bodyAngularList = {}
    end
end

-- ==============================
-- // Main GUI Variables
-- ==============================
local unitRunning = false
local bankRunning = false
local spamAllRunning = false
local selectedSlot = "Slot3"
local spawnDelay = 0.5
local spamAllDelay = 0.3

-- Create Window
local Window = Rayfield:CreateWindow({
    Name = "TBB Full Auto Farm GUI",
    LoadingTitle = "Loading GUI...",
    LoadingSubtitle = "by ScriptUser",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "GameAutoFarm",
        FileName = "Config"
    },
    Discord = {
        Enabled = false,
        Invite = "whats up chetaer!",
        RememberJoins = true
    },
    KeySystem = false
})

-- Hook Rayfield Notifications & UI update to TBB Core
TBB.onNotify = function(title, content, icon)
    Rayfield:Notify({
        Title = title,
        Content = content,
        Duration = 3,
        Image = 4483362458
    })
end

-- ==============================
-- // TABS
-- ==============================
local MainTab = Window:CreateTab("Auto Spawn", 4483362458)
local StageTab = Window:CreateTab("Auto Stage", 4483362458)
local ExploitsTab = Window:CreateTab("Exploits", 4483362458)
local SettingsTab = Window:CreateTab("Settings", 4483362458)

-- ==============================
-- // AUTO SPAWN TAB
-- ==============================
local AutoSpawnSection = MainTab:CreateSection("Auto Spawn")

local SlotDropdown = MainTab:CreateDropdown({
    Name = "Select Unit Slot",
    Options = {"Slot1", "Slot2", "Slot3", "Slot4", "Slot5", "Slot6", "Slot7", "Slot8"},
    CurrentOption = {"Slot3"},
    MultipleOptions = false,
    Flag = "SlotSelection",
    Callback = function(Option)
        selectedSlot = Option[1] or Option
        Rayfield:Notify({
            Title = "Slot Changed",
            Content = "Now using: " .. selectedSlot,
            Duration = 0.5,
            Image = 4483362458
        })
    end
})

local UnitToggle = MainTab:CreateToggle({
    Name = "Auto Spawn Unit",
    CurrentValue = false,
    Flag = "UnitToggle",
    Callback = function(Value)
        unitRunning = Value
        if Value then
            if spamAllRunning then
                spamAllRunning = false
                Rayfield:Notify({Title = "Spam All Disabled", Content = "Auto disabled to prevent conflicts", Duration = 2, Image = 4483362458})
            end
        end
    end
})

local SpamAllToggle = MainTab:CreateToggle({
    Name = "Spam All Slots",
    CurrentValue = false,
    Flag = "SpamAllToggle",
    Callback = function(Value)
        spamAllRunning = Value
        if Value then
            if unitRunning then
                unitRunning = false
                Rayfield:Notify({Title = "Single Unit Disabled", Content = "Auto disabled to prevent conflicts", Duration = 2, Image = 4483362458})
            end
        end
    end
})

local BankToggle = MainTab:CreateToggle({
    Name = "Auto Spawn Bank",
    CurrentValue = false,
    Flag = "BankToggle",
    Callback = function(Value)
        bankRunning = Value
    end
})

local ActionsSection = MainTab:CreateSection("Quick Actions")

local GiveUpButton = MainTab:CreateButton({
    Name = "Give Up Match",
    Callback = function()
        local success, err = pcall(function()
            ReplicatedStorage.Events.RemoteEvents.GiveUp:FireServer()
        end)
        if success then
            Rayfield:Notify({Title = "Give Up", Content = "Successfully surrendered match", Duration = 3, Image = 4483362458})
        else
            Rayfield:Notify({Title = "Error", Content = "Failed to give up: " .. tostring(err), Duration = 5, Image = 4483362458})
        end
    end
})

-- ==============================
-- // AUTO STAGE TAB
-- ==============================
local StageSection = StageTab:CreateSection("Stage Progression")

local StageStatusLabel = StageTab:CreateLabel("Status: ⏸  Idle")
local StageStatsLabel = StageTab:CreateLabel("W: 0  L: 0  |  ⭐  |  Diff: 1")

TBB.onStatusChange = function(text)
    StageStatusLabel:Set("Status: " .. text)
end

TBB.onStatsChange = function(text)
    StageStatsLabel:Set("Stats: " .. text)
end

local AutoStageToggle = StageTab:CreateToggle({
    Name = "Enable Auto Stage",
    CurrentValue = false,
    Flag = "AutoStageToggle",
    Callback = function(Value)
        if Value then
            TBB.start()
        else
            TBB.stop()
        end
    end
})

local ChapterInput = StageTab:CreateInput({
    Name = "Override Chapter Name (e.g. Chapter3)",
    PlaceholderText = "Leave blank for Auto",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        if Text and Text ~= "" then
            TBB.setChapter(Text)
        else
            TBB.setChapter("Auto")
        end
    end,
})

local StageInput = StageTab:CreateInput({
    Name = "Override Stage Number",
    PlaceholderText = "Leave blank for Auto",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        if Text and Text ~= "" then
            TBB.setStage(tonumber(Text) or 1)
        else
            -- if blank, let it auto detect
        end
    end,
})

local DifficultyDropdown = StageTab:CreateDropdown({
    Name = "Select Difficulty",
    Options = {"Casual", "Normal", "Tumore"},
    CurrentOption = {"Casual"},
    MultipleOptions = false,
    Flag = "DifficultySelection",
    Callback = function(Option)
        local opt = Option[1] or Option
        if opt == "Casual" then TBB.setDifficulty(1)
        elseif opt == "Normal" then TBB.setDifficulty(2)
        elseif opt == "Tumore" then TBB.setDifficulty(3)
        end
    end
})

-- ==============================
-- // EXPLOITS TAB
-- ==============================
local ProjExploitSection = ExploitsTab:CreateSection("Projectile Exploits")

local ProjToggle = ExploitsTab:CreateToggle({
    Name = "Auto Table & StoneFist modifications",
    CurrentValue = false,
    Flag = "ProjectileExploitToggle",
    Callback = function(Value)
        if Value then
            ProjectilesExploit.start()
            Rayfield:Notify({Title = "Projectiles Exploit", Content = "Enabled", Duration = 2, Image = 4483362458})
        else
            ProjectilesExploit.stop()
            Rayfield:Notify({Title = "Projectiles Exploit", Content = "Disabled", Duration = 2, Image = 4483362458})
        end
    end
})

-- ==============================
-- // SETTINGS TAB
-- ==============================
local SettingsSection = SettingsTab:CreateSection("Configuration")

local SpawnDelaySlider = SettingsTab:CreateSlider({
    Name = "Single Spawn Delay (seconds)",
    Range = {0.1, 5},
    Increment = 0.1,
    CurrentValue = 0.5,
    Flag = "SpawnDelay",
    Callback = function(Value)
        spawnDelay = Value
    end
})

local SpamAllDelaySlider = SettingsTab:CreateSlider({
    Name = "Spam All Delay (seconds)",
    Range = {0.1, 3},
    Increment = 0.1,
    CurrentValue = 0.3,
    Flag = "SpamAllDelay",
    Callback = function(Value)
        spamAllDelay = Value
    end
})

local InfoSection = SettingsTab:CreateSection("Information")
local InfoLabel1 = SettingsTab:CreateLabel("Lower delay = faster spawning but may cause issues")
local InfoLabel2 = SettingsTab:CreateLabel("Spam All cycles through all 8 slots automatically")

-- ==============================
-- // MAIN LOOPS
-- ==============================
local function spawnUnit(slot)
    local success, err = pcall(function()
        local Events = ReplicatedStorage:WaitForChild("Events", 5)
        if Events then
            local RemoteFunction = Events:WaitForChild("RemoteFunction", 5)
            if RemoteFunction then
                local PlayerSpawn = RemoteFunction:WaitForChild("PlayerSpawn", 5)
                if PlayerSpawn then
                    PlayerSpawn:InvokeServer(slot)
                end
            end
        end
    end)
    
    if not success then
        warn("Spawn error for " .. slot .. ": " .. tostring(err))
    end
    
    return success
end

task.spawn(function()
    while true do
        pcall(function()
            if not player or not player.Parent then return end
            if unitRunning and not spamAllRunning then
                spawnUnit(selectedSlot)
            end
            if bankRunning then
                spawnUnit("Bank")
            end
        end)
        task.wait(spawnDelay)
    end
end)

task.spawn(function()
    local allSlots = {"Slot1", "Slot2", "Slot3", "Slot4", "Slot5", "Slot6", "Slot7", "Slot8"}
    while true do
        if spamAllRunning then
            pcall(function()
                if not player or not player.Parent then return end
                for _, slot in ipairs(allSlots) do
                    if not spamAllRunning then break end
                    spawnUnit(slot)
                    task.wait(0.05)
                end
            end)
            task.wait(spamAllDelay)
        else
            task.wait(0.5)
        end
    end
end)

-- Notification on load
Rayfield:Notify({
    Title = "GUI Loaded",
    Content = "All Modules (Auto Spawn, Auto Stage, Exploits) active!",
    Duration = 5,
    Image = 4483362458
})
